"use client";

// Animation timing for the homepage navigation.
export const ANIMATION_TIMING = {
    // Interval between navigation item reveals in seconds.
    navStaggerSec: 0.6,
    // Navigation item entrance duration in seconds.
    navItemDurationSec: 0.6,
};

// Opacity applied to navigation items while another item is hovered.
export const HOVER_DIM_OPACITY = 0.45;

// Duration of the navigation hover dim transition in seconds.
export const HOVER_DIM_DURATION_SEC = 0.18;
